import { defineConfig } from "oxfmt"

export default defineConfig({
	semi: false,
	singleQuote: false,
	tabWidth: 2,
	useTabs: true,
	trailingComma: "all",
	printWidth: 100,
	arrowParens: "always",
})
