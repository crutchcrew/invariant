pre-commit:
  parallel: true
  jobs:
    - name: fmt
      glob: "*.{ts,tsx,js,jsx,json,css,md}"
      run: bunx oxfmt {staged_files}

    - name: lint
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx oxlint --fix {staged_files}
